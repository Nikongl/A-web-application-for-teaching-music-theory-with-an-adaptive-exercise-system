name: CI

on:
  push:
    branches: [ labwork5, main ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Build Backend
        run: docker build -t music-backend ./backend
        
      - name: Build Frontend
        run: docker build -t music-frontend ./frontend